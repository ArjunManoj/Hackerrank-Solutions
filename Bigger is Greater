#include <bits/stdc++.h>
#include <algorithm>
using namespace std;

int main(){
    int n,i;
    cin>>n;
    string s;
    while(n--){
        cin>>s;
        if(next_permutation(s.begin(),s.end())==true)
        cout<<s<<endl;
        else 
        cout<<"no answer"<<endl;
    }
}
